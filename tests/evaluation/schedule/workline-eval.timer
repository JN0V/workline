# Every Monday; a week the machine was off then runs at the next session
# (Persistent). run.sh skips it when nothing evaluated changed.
[Unit]
Description=workline evaluation, every week

[Timer]
OnCalendar=weekly
Persistent=true
RandomizedDelaySec=15m

[Install]
WantedBy=timers.target
