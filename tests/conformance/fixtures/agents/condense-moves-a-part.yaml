# The documentalist's answer to "docs/sessions.md is over its line budget" in
# the `untidy` fixture: the last section moves, whole, into a card, and a link
# to it opens the doc. Written without `diff --git` lines, as agents do.
- patch: |
    --- /dev/null
    +++ b/docs/cards/signing-out.md
    @@ -0,0 +1,3 @@
    +# Signing out
    +
    +Signing out ends the session.
    --- a/docs/sessions.md
    +++ b/docs/sessions.md
    @@ -2,2 +2,4 @@
     
    +See also [signing out](cards/signing-out.md).
    +
     ## How long a session lasts
    @@ -7,5 +9 @@ refreshed. When it expires, the user is asked to sign in again, and anything
     they had not saved is kept in the browser until they come back to the page.
    -
    -## Signing out
    -
    -Signing out ends the session.
