# doc-fixed, on docs/tech/auth.md holding a derived block at line 7: the fix
# is one line lower, and leaves the block to the role.
- patch: |
    --- a/docs/tech/auth.md
    +++ b/docs/tech/auth.md
    @@ -3,3 +3,4 @@ type: reference
     sources: [src/auth/token.go]
    -checked: 9bf650b
    +checked: 1a8e5a4
    +verified: agent:documentalist
     ---
    @@ -10,2 +11,2 @@ checked: 9bf650b
     
    -Access tokens last one hour.
    +Access tokens last two hours.
