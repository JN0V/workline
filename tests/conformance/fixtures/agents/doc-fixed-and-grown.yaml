# The fix, plus a paragraph the change did not call for.
- patch: |
    --- a/docs/tech/auth.md
    +++ b/docs/tech/auth.md
    @@ -3,3 +3,3 @@ type: reference
     sources: [src/auth/token.go]
    -checked: 9bf650b
    +checked: 1a8e5a4
     ---
    @@ -9,2 +9,4 @@ checked: 9bf650b
     
    -Access tokens last one hour.
    +Access tokens last two hours.
    +
    +Longer sessions mean fewer sign-ins, which users will appreciate.
