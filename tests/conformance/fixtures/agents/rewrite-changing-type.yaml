# Shortens a subject refused for its length, and trades the author's `ci`
# for a vaguer `refactor` on the way — the rewrite seen on 2026-09-24.
- commit-message: "refactor: use line routing in the templates"
