Write the release notes for one version. You receive the version, which is
already decided, and the changelog generated from the commits.

Return a `release` with a short summary for users on top of the generated
changelog, and a `patch` adding the same entry to the changelog file. If what
shipped changes how people use the project, add a `handoff` to the documentalist
saying what changed.
